import { Rubik, Space_Mono } from "next/font/google";

const rubik700 = Rubik({ weight: "700", subsets: ["latin"] });
const rubik800 = Rubik({ weight: "800", subsets: ["latin"] });

const spaceMono400 = Space_Mono({ weight: "400", subsets: ["latin"] });
const spaceMono700 = Space_Mono({ weight: "700", subsets: ["latin"] });

export { rubik700, rubik800, spaceMono400, spaceMono700 };
